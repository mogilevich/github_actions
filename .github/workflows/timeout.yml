name: Test Timeout
on: push

jobs:
    test-timeout:
        runs-on: ubuntu-latest
#        timeout-minutes: 1
        steps:
            - name: Sleep for 20 seconds
              run: sleep 20
              timeout-minutes: 1